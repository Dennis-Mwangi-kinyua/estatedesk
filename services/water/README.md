# @estatedesk/water

Service stub. Domain logic remains in `apps/web` until this service is extracted (see `docs/architecture/SERVICES.md`).

## Phase 1

Package exists for monorepo ownership and future imports. No independent deploy yet.

## Prisma ownership

See root `docs/architecture/SERVICES.md` for model ownership. Shared schema remains at repo root until DB-per-service cutover.
